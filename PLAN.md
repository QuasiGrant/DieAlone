# Plan

How this file works:
- Cowork writes and orders the tasks. Code does them one at a time, top to bottom.
- Code may tick a box and add lines under Rules and Tips. Code does not add, remove, reorder, or reword tasks.
- A task is finished when its done-check passes, its box is ticked, and the work is committed and pushed.
- Only the current milestone is broken into tasks. Later milestones stay as headlines until they are next.

## Milestone 1: Walk around a gray test scene

- [x] 1.1 Test scene. Create `Assets/Scenes/Graybox.unity`: flat ground about 50 m across, one tall box standing in for the tower, five or six smaller boxes of different sizes scattered around for scale, one directional light. Use Unity's built-in shapes and the default gray material. Build it through the Editor bridge or an Editor script, never by writing the scene file by hand. Done when: the scene is saved, opens with no console errors, and a screenshot shows the layout.
- [x] 1.2 Player that walks and looks. In Graybox, add a Player with a CharacterController and a camera at about 1.6 m eye height. One script, `Assets/Scripts/Player/PlayerController.cs`: walk with the Move action, look with the Look action, gravity, mouse cursor locked during play. Read input from the Player map in the existing `Assets/InputSystem_Actions.inputactions` so keyboard/mouse and gamepad both work. Walk speed slow and steady, starting at 2.5 m/s. No jump, sprint, crouch, or interact yet. Done when: clean compile, Play mode starts with no console errors, and the owner has walked around the scene with keyboard and mouse (and a gamepad if one is plugged in) and confirmed it.
- [x] 1.3 Remove template leftovers. Delete `Assets/TutorialInfo`, `Assets/Readme.asset`, and `Assets/Scenes/SampleScene.unity` with their .meta files, through the Editor so references stay clean. Make Graybox the only scene in the build list. Done when: clean compile, no console errors, Graybox still plays.

## Milestone 2: Controls and interaction

In every task below, "no errors" means no console errors from project code.

- [x] 2.1 Test course. In Graybox, add gray test pieces built from Unity's basic shapes: a long straight lane for sprinting, a low tunnel that only fits a crouched player, a low step and a log to hop over, a fence too tall to jump, a ramp, a short flight of stairs, a small room with a doorway ready for the door task, and a table and a shelf holding a few small loose objects. Build through the Editor bridge or an Editor script. Done when: the scene opens with no errors, a screenshot shows each piece, and the owner has walked the course and confirmed nothing blocks the player by accident.
- [x] 2.2 Tower with stairs and a top room. Replace the tall box with a gray tower about 10 m tall on four legs, switchback stairs up to the top, and simple rails so the player cannot walk off the edge. On top, a room about 4 by 4 m with a walkway around it, a doorway ready for the door task, window openings on all four sides, and inside a table and a bed-sized box as stand-ins for the desk and bunk. Sizes are for testing, not final. No fall damage. Done when: no errors, screenshots show the stairs and the room, and the owner has climbed to the top, walked around inside, and come back down without getting stuck.
- [x] 2.3 Sprint. Hold the Sprint action to move at 4.5 m/s instead of 2.5. No stamina limit. Done when: clean compile, no errors, and the owner has confirmed it on the sprint lane.
- [x] 2.4 Crouch. Hold the Crouch action to lower the camera and the CharacterController and move slower. The player cannot stand up under a low ceiling. Done when: clean compile, no errors, and the owner has gone through the low tunnel and confirmed they cannot stand up inside it.
- [x] 2.5 Jump. A small hop of about 0.6 m on the Jump action, only while grounded. Done when: clean compile, no errors, and the owner has confirmed the player clears the low step and the log and does not clear the fence.
- [x] 2.6 Interact base. A small dot in the center of the screen. Looking at a usable object within 2 m shows a short prompt, and the Interact action uses it. One reusable way to mark an object as usable, so doors and pickups build on it. Add one test box that reacts visibly when used. Done when: clean compile, no errors, and the owner has used the test box with keyboard and mouse.
- [x] 2.7 Doors. A hinged door that opens and shuts with Interact, built on 2.6. Place one in the test room doorway and one in the tower room doorway. The door must not shove or trap the player. Done when: clean compile, no errors, and the owner has opened and shut both doors from both sides.
- [x] 2.7a Tuning asset. Move the player, interact, and door numbers into one PlayerTuning ScriptableObject at `Assets/Settings/PlayerTuning.asset`, so they can be changed during Play mode and the changes stay after Play stops. The scripts read from the asset. Keep every current value exactly as it is now, including stepOffset 0.1, skinWidth 0.035, and sprint 5.5 m/s. Nothing about how the game plays changes in this task. Done when: clean compile, no errors, and the owner has changed jump height during Play mode, stopped, and seen the new value kept.
- [x] 2.8 Pick up and set down. Built on 2.6. Pick up one loose object, carry it in front of the player, and set it down where the player is looking. One object at a time. Not an inventory. Its tunable numbers go in the PlayerTuning asset. Done when: clean compile, no errors, and the owner has moved an object from the table to the shelf.
- [x] 2.9 Pause menu. Escape or gamepad Start pauses the game, frees the mouse cursor, and shows Resume and Quit plus two settings: look sensitivity and invert look. Settings persist between sessions. These two are player settings and do not go in the PlayerTuning asset. Before building, say which Unity UI system you will use (UI Toolkit or uGUI) and why. Done when: clean compile, no errors, and the owner has paused, changed both settings, resumed, and seen the settings kept after restarting Play mode.

## Milestone 3: VHS look on the test scene

Main inspiration is Fears to Fathom: a VHS tape look first, with a soft low-resolution picture. PS1 effects (jittery edges, wobbly textures) stay off. Every filter and shader is written for this project. No kits and no new packages. In every task below, "no errors" means no console errors and no shader compile errors. Every look number goes in one LookTuning ScriptableObject at `Assets/Settings/LookTuning.asset`, following the PlayerTuning pattern, so the owner can change it during Play mode.

- [x] 3.1 Something to look at. Give the test scene real photo textures from ambientCG or Poly Haven, CC0 only: wood planks, concrete, dirt, and painted metal. Before downloading anything, propose one texture per surface with a link to its page and wait for the owner's yes. Download the 1K JPG version and use only the color map. Store each one under `Assets/Textures/<Name>/`, add a line per texture to `Assets/Textures/SOURCES.md` with its site and asset ID, and set import Max Size to 512. Apply them to the ground, the tower, the test room, and the props. Add one warm lamp in the tower room. Add a night lighting setup and make it the scene default, keeping the daytime light as a disabled object so it can be switched back. Done when: no errors, screenshots from three fixed camera spots (inside the tower room, on the tower stairs, looking down the test course), and the owner has walked the scene at night and confirmed it is readable. Record the three camera spots under Rules and Tips so later tasks reuse them.
- [x] 3.2 Filter base. One full-screen filter wired into the game camera, with a single on and off switch, reading its numbers from the LookTuning asset. Before building, say which Unity 6000.3 URP feature you will build on (for example a Full Screen Pass Renderer Feature or a custom ScriptableRendererFeature using Render Graph), confirm it exists in this version, and say why. First effect: a soft low-resolution picture, with the internal height as a number (start near 480). The HUD dot, the prompt, and the pause menu must stay readable. Done when: no errors, before and after screenshots from the three camera spots, and the owner has toggled the filter in Play mode and changed the resolution number live.
- [x] 3.3 Tape color. Color bleed (colors smearing sideways), slightly washed-out colors, and crushed blacks, each with its own strength number. Done when: no errors, screenshots from the three spots, and the owner has tuned each number in Play mode.
- [x] 3.4 Grain and tape noise. Moving grain, plus an occasional noise band rolling up the screen. Strength, speed, and how often the band appears are numbers. Done when: no errors, screenshots, and the owner has confirmed it in motion in Play mode.
- [x] 3.5 Scan lines, blur, and dark corners. Each with its own strength number, and each can be set to zero. Done when: no errors, screenshots, and the owner has tuned each in Play mode.
- [x] 3.6 Fog and darkness. Distance fog tuned so the far end of the test course fades out, with fog color and distance as numbers. The fog must work together with the filter. Done when: no errors, screenshots, and the owner has confirmed it at night from the top of the tower and on the test course.
- [x] 3.7 Tuning pass. The owner tunes every look number by eye from the three camera spots, with Fears to Fathom screenshots open beside the game. Code helps on request and changes no code unless something is broken. Done when: the owner says the look is right, the final values are saved in the LookTuning asset, and final screenshots from the three spots are committed under `Docs/Look`.

## Milestone 4: Scene setup

Make it safe and repeatable to add scenes. Graybox is kept as the permanent test scene. In every task below, "no errors" means no console errors from project code.

- [x] 4.1 Housekeeping. Add `UpgradeLog.htm` to .gitignore and delete `shot.png` from the project root. Done when: `git status` is clean and the commit is pushed.
- [x] 4.2 Tag the Graybox state. Create the git tag `graybox-m3` on commit 31fa880 and push it. Done when: the tag is visible on GitHub.
- [x] 4.3 Prefabs. List the objects in Graybox that make up the player, the game systems, the HUD and pause menu, and the night lighting rig, then wait for the owner's yes. Turn each into a prefab under `Assets/Prefabs/` and make Graybox use the prefabs. Done when: no errors, and the owner has played Graybox and confirmed it plays exactly as before (walk, sprint, crouch, jump, doors, carry, pause menu, VHS look).
- [ ] 4.4 New scene recipe. One documented way to make a new scene that starts with the prefabs, night lighting, fog, the look filter, and a place in the build list. Before building, say whether Unity's built-in Scene Template feature does this in 6000.3, confirm it exists in this version, and if it does not fit, propose an Editor menu item instead. Write the recipe as one line under Rules and Tips. Done when: no errors, and a throwaway scene `Assets/Scenes/Throwaway.unity` made from the recipe plays with the player, pause menu, and VHS look working. Keep Throwaway until 4.6 is done.
- [ ] 4.5 Dev menu. F1 toggles a dev panel that lists every scene in the build list, and picking one loads it. The player and pause menu keep working after the switch. Same uGUI approach as the pause menu. The dev menu must not exist in release builds: compile it out with `#if UNITY_EDITOR || DEVELOPMENT_BUILD`, and prove it by making a non-development build and showing that F1 does nothing in it. Done when: no errors, the owner has jumped from Graybox to Throwaway and back in Play mode, and the release build check passed.
- [ ] 4.6 Clean up. Delete `Assets/Scenes/Throwaway.unity` and its .meta through the Editor and remove it from the build list. Done when: no errors, Graybox still plays, and the build list holds only real scenes.

## Milestone 5: New scene

The first real scene beyond Graybox. Contents to be decided by the owner before this milestone is broken into tasks.

## Milestone 6: One full day of the loop, no events

Wake, chores, check the fire, report, maintain the Ward, sleep. HP, MIND, and WARD working. Autosave at sleep.

## Later, from DESIGN.md

Main menu with New run and Continue comes after Milestone 6, since it depends on saving. Volume, graphics, and control rebinding settings come once there is sound and the VHS look to adjust.

Event deck and tracks, chapters and endings, voices, scrapbook, inventory, map, and Ward as physical objects, Ahmee, minigames.

## Open decisions (owner)

- Level blocking tool: Unity's built-in shapes or the ProBuilder package. Needed before the real tower area is blocked out. Not needed for Milestone 1.

## Rules and Tips

Lessons from finished tasks that later tasks need. One line each, newest at the bottom.
- Editor eval (eval_file / unity command eval) wraps code in a method body: no `using` lines, use fully qualified names like UnityEditor.SceneManagement.EditorSceneManager. Screenshot save_path lands under Assets/, so save to the scratchpad copy and delete the Assets/Temp asset through the Editor afterward.
- Play mode does not tick while the Editor is unfocused. During bridge-driven play checks, eval `UnityEngine.Application.runInBackground = true` (runtime only, not saved) before waiting on frames.
- Deleting scripts through the Editor triggers a domain reload that drops the bridge mid-batch; wait for editor_status ready and retry the remaining calls. Build-list edits stay in memory until `UnityEditor.AssetDatabase.SaveAssets()` writes ProjectSettings/EditorBuildSettings.asset.
- Course geometry in Graybox: tunnel clearance 1.2 m, step 0.4 m, log 0.4 m, fence 1.5 m, ramp 14 degrees to a 1.5 m platform, stairs 0.25 rise 0.3 run, room doorway 1.0 x 2.1 m. For per-piece screenshots, render a temporary hidden camera to a RenderTexture from eval and write PNGs to the scratchpad; it leaves the scene clean.
- Tower layout: deck top at y 10, room interior 4 x 4 centered at (0, 10, 12), doorway in the south wall centered at x -1.1 (1.0 x 2.1 m), stair entry at ground on the north lane at x -1.5, z 8.4. Stacked stair flights must be slabs, not ground-to-top solids, or the upper flight swallows the lower one.
- Sprint speed is 5.5 m/s in code, owner raised it from the 4.5 in task 2.3 after testing. Cowork to record in DECISIONS.md.
- Player capsule: stand 1.8 m, crouch 1.0 m, radius 0.35, eye 1.6 / 0.8. Stand-up check is a SphereCast up from the crouched capsule top; to test it from eval, teleport the Player (disable the CharacterController around the position set, then Physics.SyncTransforms) and invoke HasHeadroom by reflection.
- STAIRS RULE: the Player CharacterController has stepOffset 0.1 and skinWidth 0.035, so it cannot climb steps. Every staircase gets a collider-only sloped box (a GameObject named StairRamp with just a BoxCollider) whose top face runs along the tread nosings, from one run below the first nosing to the top nosing. See Tower/Stairs/Flight1/StairRamp for the pattern. Reason: with stepOffset 0.3 the capsule slid up over any 0.35 to 0.65 m edge when moving sideways, so hop-only obstacles were climbable without jumping.
- Interaction: derive from Interactable (Assets/Scripts/Interaction), set the prompt string, override Use. PlayerInteractor on the Player raycasts 2 m from the camera and calls Use on the Interact action. HUD uses uGUI with legacy Text; TextMeshPro needs its Essential Resources imported first, which is an asset add. The center dot is HUD/Dot, active; the owner turned it off once and then asked for it back.
- Doors: Door component on a hinge object at the west jamb, Panel child cube 0.96 x 2.08 x 0.08 with the collider, kinematic Rigidbody on the hinge. Opens away from the user by the sign of dot(hinge.forward, toUser). Moving colliders that the player can touch should be driven this way, with an overlap check against the CharacterController before each step so they wait instead of pushing.
- Tunables: every feel number lives in Assets/Settings/PlayerTuning.asset (class PlayerTuning). New numbers for player, interaction, doors, and carrying go there as public fields with a Header, and scripts take a [SerializeField] PlayerTuning tuning reference wired in the scene. Capsule values are applied once in PlayerController.Awake.
- Carrying: Carryable on the object (needs a Collider and a Rigidbody), PlayerCarry on the Player with HoldPoint under the camera. Set down needs a surface with normal.y >= placeMinUpNormal within placeReach and a clear OverlapBox; otherwise E drops with physics and shows no prompt. Objects released before the follow lerp settles are released where they are. PlayerController.OnControllerColliderHit shoves non-kinematic bodies.
- Pause and settings: GamePause on the Game object owns timeScale, cursor, and which behaviours sleep while paused. PlayerSettings (Assets/Scripts/Settings) is a data class plus JSON save/load in persistentDataPath; add a field there and a control plus Bind method in PauseMenu. Menu rows live under PauseMenu/Panel/Content as a vertical stack. Input actions are added to InputSystem_Actions through the Editor API (asset.Disable, AddAction, AddBinding, write ToJson, ImportAsset, asset.Enable), never by hand. Throw is bound to right mouse and gamepad right trigger.
- Look camera spots for Milestone 3 screenshots, position then look-at point: A tower room (0.8, 11.6, 10.6) to (-1.0, 10.9, 13.4); B tower stairs (-2.6, 6.6, 7.7) to (2.0, 7.8, 8.4); C test course (-17, 1.6, 12) to (-17, 0.5, -20). Render them with a temporary camera copied from Camera.main (scratchpad shoot_look.cs pattern). Night is the scene default: Night/Moon directional light plus flat ambient, daytime "Directional Light" kept disabled. Materials are shared by texture and tiling under Assets/Materials; the tiling comes from each object scale, so resizing an object means reassigning its material.
- Look filter: LookFilterFeature (Assets/Scripts/Look) is a Render Graph ScriptableRendererFeature on Assets/Settings/PC_Renderer.asset, game camera only, BeforeRenderingPostProcessing. It blits camera color to a low-res texture and back through Assets/Shaders/LookFilter.shader pass 0. New effects: add a field to LookTuning, a property and a few lines to the shader, and set the property on the material in RecordRenderGraph. The shader must include URP Core.hlsl before Blit.hlsl or TEXTURE2D_X is undefined. Renderer features are added to the renderer asset through SerializedObject on m_RendererFeatures and m_RendererFeatureMap plus AddObjectToAsset (scratchpad wire_look.cs pattern). Shader compile check: UnityEditor.ShaderUtil.ShaderHasError.
- Look shader color math: the camera color is linear HDR at the filter point. Anything that judges darkness (black point, levels) must convert with LinearToSRGB, act, and convert back, or a small number looks huge. Saturation and chroma math are fine in linear.
- Look shader noise: use the PCG integer hash in LookFilter.shader for anything random per pixel; fractional hashes show a weave on whole-number inputs. Time comes in as _LookTime (unscaled) from the feature, so effects keep moving while paused. Fears to Fathom reference: grain subtle (about 0.15 to 0.25), bands rare or scripted.
- Scene-side look settings (fog, sky color) are applied every frame by LookEnvironment on the Game object from LookTuning. Add ambient or exposure there the same way rather than editing RenderSettings in the scene by hand.
- Final Milestone 3 look values live in Assets/Settings/LookTuning.asset; reference screenshots from the three spots are in Docs/Look (PNG, via LFS). Re-shoot with the shoot_look_final.cs pattern after any look change the owner signs off.
- Prefabs (Assets/Prefabs): Player, GameSystems (Game + EventSystem + HUD + PauseMenu), NightLighting (Moon + disabled Sun). Links that cross prefabs (GamePause behaviours, PlayerInteractor promptUI, LookEnvironment camera) resolve at startup by FindFirstObjectByType when left empty, so a new scene only needs the three prefabs dropped in. Graybox keeps explicit links as instance overrides. Never edit RenderSettings by hand: ambient is per scene, fog comes from LookTuning via LookEnvironment.
- NEW SCENE RECIPE: in Unity use the menu DieAlone > New Scene From Base, pick a name under Assets/Scenes, done. It instantiates Assets/Scenes/Templates/DieAloneBase.scenetemplate (built from Templates/Base.unity: Player, GameSystems, NightLighting prefabs, a 50 m dirt ground, night ambient, fog) and adds the scene to the build list. From code call NewSceneMenu.CreateScene(path) (Assets/Editor/NewSceneMenu.cs). To change what every new scene starts with, edit Base.unity and re-run the template creation; the template references assets, it never clones them.
- InputSystemUIInputModule in a prefab must have its action fields (point, leftClick, submit, cancel...) set to the InputActionReference sub-assets of InputSystem_Actions.inputactions (LoadAllAssetRepresentationsAtPath, names like "UI/Point"). Assigning only actionsAsset creates scene-local references that a prefab drops, and menus stop taking clicks in new scenes. To test a UI click from eval, add a virtual Mouse device and spread move, press, release over separate frames.
